export interface User {
  id: number;
  firstName: string;
  lastName: string;
  companyName: string;
  email: string;
  confirmEmail: string;
  newPassword: string;
  confirmPassword: string;
  currentPassword: string;
}
